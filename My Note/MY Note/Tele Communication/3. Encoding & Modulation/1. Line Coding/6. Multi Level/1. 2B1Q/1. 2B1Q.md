# 2B1Q voltage level mapping
2B1Q = **2 Binary, 1 Quaternary**

উদাহরণ mapping:

|Bits|Level|
|---|---|
|00|-3V|
|01|-1V|
|10|+1V|
|11|+3V|

একটি symbol-এ 2 bit পাঠানো হয়।


![[Pasted image 20260606150006.png]]


---

# 2B1Q Recap

## 1. Synchronization Problem ✅

এটি সত্য।

ধরো data:

```text
00 00 00 00 00
```

Signal:

```text
-3 -3 -3 -3 -3
```

কোন transition নেই।

---

আবার:

```text
11 11 11 11
```

Signal:

```text
+3 +3 +3 +3
```

এখানেও transition নেই।

---

Receiver সাধারণত transition দেখে clock recover করে।

Transition না থাকলে:

```text
Clock Recovery Problem
↓
Synchronization Problem
```

তাই 2B1Q self-synchronizing নয়।

---

## 2. DC Component Problem ✅

এটাও সত্য।

ধরো অনেকক্ষণ:

```text
11 11 11 11
```

পাঠানো হচ্ছে।

Signal:

```text
+3 +3 +3 +3 +3
```

Average voltage positive হয়ে যাবে।

---

আবার:

```text
00 00 00 00
```

Signal:

```text
-3 -3 -3 -3
```

Average voltage negative হয়ে যাবে।

---

AMI-তে যেমন:

```text
+ - + - + -
```

alternate polarity থাকায় DC component cancel হয়।

কিন্তু 2B1Q-তে এমন কোনো rule নেই।

ফলে:

```text
Long run of same symbols
↓
Non-zero average voltage
↓
DC Component
```

---

## 3. Error Detection ❌

এটাও সঠিক।

AMI-তে:

```text
+ - + -
```

এরপর যদি receiver পায়:

```text
+ - + +
```

তাহলে সঙ্গে সঙ্গে বুঝবে:

> Bipolar Violation হয়েছে।

---

2B1Q-তে:

```text
	00 → -3V
	01 → -1V
	10 → +1V
	11 → +3V
```

প্রতিটি symbol স্বাধীন।

কোন alternating rule নেই।

কোন violation rule নেই।

---

তাই:

```text
+1V
```

noise-এর কারণে

```text
+3V
```

হয়ে গেলে receiver শুধু ভুল data decode করবে।

কিন্তু বুঝতে পারবে না error হয়েছে।

---

# Summary Table

|Problem|2B1Q|
|---|---|
|DC Component|✅ হতে পারে|
|Synchronization Problem|✅ আছে|
|Error Detection Capability|❌ নেই|
|Bandwidth Efficiency|✅ ভালো|
|Receiver Complexity|✅ বেশি (4 level distinguish করতে হয়)|
|Noise Sensitivity|✅ বেশি (level gap ছোট)|

---

পরীক্ষা বা viva-তে সংক্ষেপে বলতে পারো:

> **2B1Q reduces bandwidth by encoding 2 bits per signal element, but it suffers from synchronization problems during long runs of identical symbols, may introduce DC components due to voltage imbalance, and provides no inherent error detection mechanism.**

